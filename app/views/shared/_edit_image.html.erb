<%= form_with(model: @image, url: image_path(@image), method: :patch) do |form| %>
  <div>
    <%= form.label :title %>
    <%= form.text_field :title, placeholder: 'Enter new image title' %>
  </div>

  <div>
    <%= form.submit 'Update image title', class: "cursor-pointer" %>
  </div>
<% end %>
